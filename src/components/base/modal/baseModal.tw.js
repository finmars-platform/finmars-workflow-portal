export default {
    height: {
        '--modalHeader-height': 'var(--modalHeader-height)',

        '--modalFooter-height': 'var(--modalFooter-height)',
    },
    backgroundColor: {
        '--modalHeader-backgroundColor': 'var(--modalHeader-backgroundColor)',
        '--modalContent-backgroundColor': 'var(--modalContent-backgroundColor)',
        '--modalFooter-backgroundColor': 'var(--modalFooter-backgroundColor)',
    },
    borderRadius: {
        '--modal-borderRadius': 'var(--modal-borderRadius)',
    },
    fontSize: {
        '--modalTitle-fontSize': 'var(--modalTitle-fontSize)',
    },
    fontFamily: {
        '--modalTitle-fontFamily': 'var(--modalTitle-fontFamily)',
    },
    fontWeight: {
        '--modalTitle-fontWeight': 'var(--modalTitle-fontWeight)',
    },
    lineHeight: {
        '--modalTitle-lineHeight': 'var(--modalTitle-lineHeight)',
    },
    letterSpacing: {
        '--modalTitle-letterSpacing': 'var(--modalTitle-letterSpacing)',
    }
};
